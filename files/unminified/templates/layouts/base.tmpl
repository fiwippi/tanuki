<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ block "title" . }}{{ end }}</title>
    <link rel="stylesheet" href='{{versioning "/static/css/style.css"}}'>
    <link rel="stylesheet" href='{{versioning "/static/css/fira_mono.css"}}'>
    <script src='{{versioning "/static/js/theme.js"}}'></script>
    <script type="module">
        import * as API from {{ versioning "/static/js/api.js" }}

        document.addEventListener('alpine:init', () => {
            Alpine.data('logout', () => ({
                async logout() {
                    await API.Auth.Logout()
                    window.location.replace('/login')
                }
            }))
        })
    </script>
    {{ block "styles" . }}{{ end }}
    {{ block "scripts" . }}{{ end }}
    <script defer src='{{versioning "/static/js/alpine.js"}}'></script>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1><a class="no-bottom" href="/">Tanuki</a></h1>
            → <a href="/">Home</a>
            → <a href="/tags">Tags</a>
            <span x-data="{ admin: {{ admin . }} }" x-show="admin" x-cloak>
                → <a href="/admin">Admin</a>
            </span>
            → <a x-data="logout" @click="await logout()" href="javascript:void(0)">Logout</a>
            <button x-data style="float: right" @click="toggleTheme()">Change Theme 🎨</button>
            <br><br>
        </div>
        <div class='main'>
            {{ block "content" . }}{{ end }}
        </div>
    </div>
</body>
</html>